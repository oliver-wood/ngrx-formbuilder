import { BasicInformationFormHandler } from './basic-information-form-handler';

describe('BasicInformationFormHandler', () => {
  it('should create an instance', () => {
    expect(new BasicInformationFormHandler()).toBeTruthy();
  });
});
